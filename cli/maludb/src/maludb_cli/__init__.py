"""MaluDB first-party CLI — V3-CLI-01.

Subcommands call the stable SQL/HTTP/MC2DB APIs; no business logic
lives in the CLI itself. Output supports human text (default) and
JSON modes.
"""

__version__ = "0.1.0"
