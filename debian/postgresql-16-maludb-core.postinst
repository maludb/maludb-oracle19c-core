#!/bin/sh
# Postinst for postgresql-16-maludb-core.

set -e

case "$1" in
    configure)
        echo "MaluDB core extension files installed for PostgreSQL 16."
        echo "To enable in a database:"
        echo "    sudo -u postgres psql -d <db> -c 'CREATE EXTENSION maludb_core CASCADE;'"
        ;;
    abort-upgrade|abort-remove|abort-deconfigure)
        ;;
esac

#DEBHELPER#
exit 0
