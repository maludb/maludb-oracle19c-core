# V4 Bench Reference Document

A small multi-level markdown fixture for the PageIndex bench
harness. Each topic's summary mentions the cue tokens its
descendant leaves use, so the deterministic `overlap` descent
strategy can find the right path from root to leaf without an
LLM in the loop.

## Background

Background covers alpha and beta concepts only. Pre-history of
theta encoding. Strictly alpha-beta material; no procedural steps.

## Methods

Methods chapter covering the gamma delta epsilon zeta iota kappa
lambda procedural step set. Multiple sub-steps live here.

### Step One — gamma capture

Step one covers the gamma delta procedure. Capture data in three
phases. Each phase produces an epsilon value used downstream.

### Step Two — zeta handling

Step two handles zeta artifacts. Convert from epsilon to zeta via
the iota transform.

### Step Three — kappa validation

Step three validates against kappa baselines. Reject if lambda
deviates by more than three sigma.

## Results

Results section. Mu through omicron. Pi metrics meet baseline.

## Discussion

Discussion of rho sigma tau upsilon phi chi correlations under
load. Tau peaks coincide with upsilon spikes.

## Conclusion

The omega chapter closes the loop. Conclusion summary.
